<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="br.temporeal.login"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name" >
    <!-- android:theme="@android:style/Theme_Light" -->
        <activity android:name=".TelaLogin"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TelaBemVindo" />
        <activity android:name=".TelaListaCarros" />
        <activity android:name=".TelaFormularioCarro" />

    </application>
    <uses-sdk android:minSdkVersion="3" />

</manifest> 